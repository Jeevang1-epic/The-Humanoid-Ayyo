[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ayyo-physical-camera"
version = "0.1.0"
description = "Driver-neutral physical camera calibration and lifecycle boundary for Ayyo"
requires-python = ">=3.12"
license = {text = "Proprietary"}
maintainers = [
  {name = "P. Jeevan Kumar", email = "jeevan116p@gmail.com"},
]
dependencies = [
  "ayyo-world-model==0.1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
