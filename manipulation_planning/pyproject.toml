[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ayyo-manipulation-planning"
version = "0.1.0"
description = "Deterministic planning-only left-arm evidence boundary for Ayyo"
requires-python = ">=3.12"
license = {text = "Proprietary"}
maintainers = [
  {name = "P. Jeevan Kumar", email = "jeevan116p@gmail.com"},
]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
