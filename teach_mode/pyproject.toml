[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ayyo-teach-mode"
version = "0.1.0"
description = "Bounded deterministic demonstration capture evidence for Ayyo"
requires-python = ">=3.12"
license = {text = "Proprietary"}
maintainers = [
  {name = "P. Jeevan Kumar", email = "jeevan116p@gmail.com"},
]
dependencies = [
  "ayyo-developmental-scenarios==0.1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
