[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ayyo-manipulation-grasp-interaction"
version = "0.1.0"
description = "Simulation-only Stage 9D grasp interaction evidence boundary for Ayyo"
requires-python = ">=3.12"
license = {text = "Proprietary"}
maintainers = [
  {name = "P. Jeevan Kumar", email = "jeevan116p@gmail.com"},
]
dependencies = [
  "ayyo-manipulation-simulation-execution==0.1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
